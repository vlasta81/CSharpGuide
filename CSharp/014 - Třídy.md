
### **1. Definice tøídy** 

- **Tøída** je základní stavební jednotka objektovì orientovaného programování (OOP).  
- Slouží jako **šablona** pro vytváøení objektù, které obsahují **data** (pole, vlastnosti) a **chování** (metody).  
- **Ètyøi pilíøe OOP**:  
  - **Zapouzdøení** (encapsulation)  
  - **Dìdiènost** (inheritance)  
  - **Polymorfismus** (polymorphism)  
  - **Abstrakce** (abstraction).

---

### **2. Struktura tøídy**  

#### **a) Základní komponenty**  

```csharp
public class Uzivatel 
{
    // Pole (data)
    private string _jmeno;

    // Vlastnost (property)
    public string Jmeno 
    {
        get => _jmeno;
        set => _jmeno = value ?? "Neznámý";
    }

    // Konstruktor
    public Uzivatel(string jmeno) 
    {
        Jmeno = jmeno;
    }

    // Metoda
    public void PredstavSe() 
    {
        Console.WriteLine($"Jsem {Jmeno}");
    }
}
```

#### **b) Modifikátory pøístupu**  

- `public`: Pøístupný odkudkoli.  
- `private`: Pøístupný pouze uvnitø tøídy (výchozí pro èleny).  
- `protected`: Pøístupný uvnitø tøídy a odvozených tøíd.  
- `internal`: Pøístupný v rámci assembly.  

#### **c) Dìdiènost**  

- Tøída mùže dìdit od **jedné** základní tøídy a implementovat **více rozhraní**.  
```csharp
public class Admin : Uzivatel, IEditujici 
{
    public Admin(string jmeno) : base(jmeno) { }

    public void UpravDatabazi() 
    {
        Console.WriteLine("Databáze upravena.");
    }
}
```

#### **d) Abstraktní tøídy a rozhraní**  

- **Abstraktní tøída**: Nelze instancovat, obsahuje abstraktní metody.  
- **Rozhraní**: Definuje kontrakt (metody/vlastnosti bez implementace).  
```csharp
public abstract class Zvire 
{
    public abstract void Zvuk();
}

public interface ILetajici 
{
    void Let();
}
```

---

### **3. Dùležité koncepty**  

#### **a) Konstruktory a destruktory**  

- **Konstruktor**: Inicializuje objekt (mùže být pøetížen).  
- **Destruktor**: Volán pøi uvolnìní objektu z pamìti (GC).  
```csharp
public class Auto 
{
    public Auto() { } // Implicitní konstruktor
    public Auto(string model) { Model = model; }

    ~Auto() { } // Destruktor (vzácnì používaný)
}
```

#### **b) Statické èleny**  

- Patøí tøídì, ne instanci.  
```csharp
public class Matematika 
{
    public static double Pi = 3.14;
    public static int Secti(int a, int b) => a + b;
}

// Použití: Matematika.Secti(5, 3);
```

#### **c) Polymorfismus**  

- **Pøetížení metod** (overloading): Stejný název, rùzné parametry.  
- **Pøepsání metod** (overriding): Zmìna implementace v odvozené tøídì.  
```csharp
public class Tvar 
{
    public virtual void Vykresli() => Console.WriteLine("Základní tvar");
}

public class Kruh : Tvar 
{
    public override void Vykresli() => Console.WriteLine("Kruh");
}
```

#### **d) Vlastnosti (Properties)**  

- Kontrola pøístupu k datùm (gettery/settery).  
```csharp
private int _vek;
public int Vek 
{
    get => _vek;
    set => _vek = value >= 0 ? value : 0;
}
```

---

### **4. Pokroèilá témata**  

#### **a) Indexery**  

- Umožòují pøístup k objektu jako k poli.  
```csharp
public class Seznam 
{
    private string[] _prvky = new string[10];
    public string this[int index] 
    {
        get => _prvky[index];
        set => _prvky[index] = value;
    }
}

// Použití: seznam[0] = "Ahoj";
```

#### **b) Generické tøídy**  

- Práce s rùznými typy za bìhu.  
```csharp
public class Box<T> 
{
    public T Obsah { get; set; }
}

// Použití: Box<int> box = new Box<int>();
```

#### **c) Statické tøídy**  

- Nelze instancovat (obsahuje pouze statické èleny).  
```csharp
public static class Utility 
{
    public static string Formatuj(string text) => text.ToUpper();
}
```

---

### **5. Porovnání tøíd a struktur**  

| **Kritérium**       | **Tøída**               | **Struktura**            |  
|----------------------|-------------------------|--------------------------|  
| **Typ**              | Referenèní              | Hodnotový                |  
| **Dìdiènost**        | Podporuje               | Nepodporuje              |  
| **Výchozí konstruktor** | Není povinný         | Automatický (nelze definovat) |  
| **Pamì**            | Halda (heap)            | Zásobník (stack)         |  

---

### **6. Bìžné chyby a tipy**  

- **Nesprávná zapouzdøení**: Veøejné pole místo vlastnosti.  
- **Zbyteèné dìdiènosti**: Preferujte kompozici pøed dìdièností.  
- **Zapomenutý `override`**: Metoda nebude pøepsána.  
- **Singleton pattern**:  
  ```csharp
  public class Singleton 
  {
      private static Singleton _instance;
      private Singleton() { }
      public static Singleton Instance => _instance ??= new Singleton();
  }
  ```

---

### **7. Ukázky kódu**  

#### **Dìdiènost a polymorfismus**  

```csharp
public abstract class Zvire 
{
    public abstract void Zvuk();
}

public class Pes : Zvire 
{
    public override void Zvuk() => Console.WriteLine("Haf!");
}

// Použití:
Zvire zvire = new Pes();
zvire.Zvuk(); // "Haf!"
```

#### **Implementace rozhraní**  

```csharp
public interface IUlozitelny 
{
    void UlozDoSouboru(string cesta);
}

public class Dokument : IUlozitelny 
{
    public void UlozDoSouboru(string cesta) 
    {
        File.WriteAllText(cesta, "Obsah");
    }
}
```

---

### **8. Doporuèené zdroje**  

- **Oficiální dokumentace**: [Classes in C#](https://learn.microsoft.com/cs-cz/dotnet/csharp/programming-guide/classes-and-structs/classes)  
